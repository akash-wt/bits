import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function SettingScreen() {
  <SafeAreaView>
    <View>
      <Text>your all settings will apear here</Text>
    </View>
  </SafeAreaView>
}
